pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/oog00001/dockerBakendTFM.git'
            }
        }

        stage('Build & Deploy') {
            steps {
                sh 'docker-compose up -d --build'
            }
        }
    }
}